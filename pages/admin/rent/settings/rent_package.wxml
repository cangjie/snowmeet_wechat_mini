<view>
  <tree
    dataTree="{{dataTree}}"
    selectKey="{{selectedCode}}"
    bind:select="handleSelect"
    isSelectLastNode="true"
    isOpenAll="{{false}}"
    checkBox="{{true}}"
  ></tree>
</view>