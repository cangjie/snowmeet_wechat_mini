<view class="fnb-page">
  <view class="fnb-head"><view class="fnb-sub">一级分类 / 二级分类</view><view class="fnb-date">{{todayLabel}}</view></view>
  <view wx:if="{{blocked}}" class="fnb-blocked">{{blocked}}</view>
  <block wx:else>
    <view class="fnb-note">二级分类只定<text class="em">名称</text>和<text class="em">建议储存方式</text>。计量单位、临期提醒、开封后默认和保质期规则跟着<text class="em">具体食材</text>走，点食材即可设置。<text wx:if="{{!isManager}}">维护需要店长权限。</text></view>
    <view wx:if="{{isManager}}" class="btn primary btn-block" bindtap="addL1">＋ 新增一级分类</view>
    <view wx:for="{{groups}}" wx:for-item="g" wx:key="id" class="card">
      <view class="card-pad card-row" data-id="{{g.id}}" bindtap="onToggle">
        <view class="grow col"><view class="card-title">{{g.name}}</view><view class="card-meta">{{g.meta}}</view></view>
        <view class="caret">{{openL1 === g.id ? '⌃' : '⌄'}}</view>
      </view>
      <view wx:if="{{openL1 === g.id}}" class="sub-list">
        <view wx:for="{{g.subs}}" wx:for-item="s" wx:key="id" class="l2">
          <view class="card-row">
            <view class="grow card-row"><text class="card-title small">{{s.name}}</text><text class="tag blue">{{s.storage}}</text></view>
            <view wx:if="{{isManager}}" class="btn" data-id="{{s.id}}" bindtap="editL2">编辑</view>
          </view>
          <view class="mats">
            <view wx:for="{{s.items}}" wx:for-item="m" wx:key="id" class="tag {{m.prepared ? 'ok' : 'muted'}} mat" data-id="{{m.id}}" bindtap="editMaterial">{{m.name}}{{m.prepared ? '（半成品）' : ''}}</view>
            <view wx:if="{{isManager}}" class="tag blue mat" data-id="{{s.id}}" bindtap="addMaterial">＋ 食材</view>
            <text wx:if="{{!s.items.length && !isManager}}" class="card-meta">暂无食材</text>
          </view>
        </view>
        <view wx:if="{{isManager}}" class="sub-row"><view class="btn" data-id="{{g.id}}" bindtap="addL2">＋ 二级分类</view>
          <view class="grow"></view><view class="btn danger" data-id="{{g.id}}" bindtap="onDeleteL1">删除一级分类</view></view>
        <view wx:if="{{!g.subs.length && !isManager}}" class="fnb-empty">暂无二级分类</view>
      </view>
    </view>
    <view wx:if="{{!loading && groups.length === 0}}" class="fnb-empty">还没有分类{{isManager ? '，先新增一级分类' : '，请店长先建分类'}}</view>
  </block>
</view>

<van-popup show="{{editShow}}" position="bottom" round bind:close="closeEditor">
  <view class="sheet" wx:if="{{edit}}">
    <view class="sheet-title">{{edit.id ? '编辑二级分类' : '新增二级分类'}}</view>
    <view class="field"><view class="field-label">名称</view><input class="input" value="{{edit.name}}" data-field="name" bindinput="setEdit" placeholder="如：速冻面点" /></view>
    <view class="field"><view class="field-label">建议储存方式<text class="hint">入库时默认选中，仍可改</text></view>
      <view class="seg"><view wx:for="{{storages}}" wx:key="code" class="chip-sq {{edit.defaultStorage === item.code ? 'on' : ''}}" data-field="defaultStorage" data-value="{{item.code}}" bindtap="setEdit">{{item.label}}</view></view></view>
    <view class="btn primary btn-block {{saving ? 'disabled' : ''}}" bindtap="saveEditor">{{saving ? '保存中…' : '保存'}}</view>
    <view wx:if="{{edit.id}}" class="btn danger btn-block" bindtap="onDeleteL2">删除该分类</view>
  </view>
</van-popup>

<van-popup show="{{matShow}}" position="bottom" round custom-style="max-height:88%;" bind:close="closeMat">
  <scroll-view scroll-y class="editor" wx:if="{{mat}}">
    <view class="sheet">
      <view class="sheet-title">{{mat.id ? '编辑食材' : '新增食材'}} · {{mat.categoryName}}</view>
      <view class="field"><view class="field-label">名称</view><input class="input" value="{{mat.name}}" data-field="name" bindinput="setMat" placeholder="如：袋装速冻饺子" /></view>
      <view class="field"><view class="field-label">类型</view>
        <view class="seg"><view class="chip-sq {{mat.itemType === 'raw' ? 'on' : ''}}" data-field="itemType" data-value="raw" bindtap="setMat">原料</view>
          <view class="chip-sq {{mat.itemType === 'prepared' ? 'on' : ''}}" data-field="itemType" data-value="prepared" bindtap="setMat">半成品（靠制作产出）</view></view></view>
      <view class="field"><view class="field-label">计量单位（默认录入单位）<text class="hint">{{mat.id ? '只能在同一计量方式内切换' : '决定库存基本单位，入库后不能跨克/毫升/个'}}</text></view>
        <view class="chips wrap"><view wx:for="{{unitOpts}}" wx:key="code" class="chip-sq {{mat.inputUnit === item.code ? 'on' : ''}}" data-field="inputUnit" data-value="{{item.code}}" bindtap="setMat">{{item.label}}</view></view></view>
      <view class="field"><view class="field-label">临期提前提醒（天）<text class="hint">生鲜 1 天、冻品 7 天、干货 30 天</text></view>
        <input class="input" type="number" value="{{mat.warnDays}}" data-field="warnDays" bindinput="setMat" /></view>
      <view class="field"><view class="field-label">封装品开封后默认<text class="hint">成袋、成瓶入库时带出，散装不用</text></view>
        <view class="input-row">
          <view class="seg grow"><view wx:for="{{storages}}" wx:key="code" class="chip-sq {{mat.openStorage === item.code ? 'on' : ''}}" data-value="{{item.code}}" bindtap="pickOpenStorage">{{item.label}}</view></view>
          <input class="input days" type="number" value="{{mat.openDays}}" data-field="openDays" bindinput="setMat" placeholder="天" />
        </view></view>
      <view class="fnb-section-title">未开封保质期规则<text class="side">按生产月份分档：6–9 月为高温档</text></view>
      <view wx:for="{{storages}}" wx:for-item="st" wx:key="code" class="rule-box">
        <view class="card-row"><text class="rule-name">{{st.label}}</text>
          <view class="seg grow"><view wx:for="{{modes}}" wx:for-item="md" wx:key="code" class="chip-sq {{mat.rules[st.code].mode === md.code ? 'on' : ''}}" data-field="rules.{{st.code}}.mode" data-value="{{md.code}}" bindtap="setMat">{{md.label}}</view></view></view>
        <view wx:if="{{mat.rules[st.code].mode === 'custom'}}" class="warn-line">该储存方式为逐月自定义规则，这里不修改</view>
        <view wx:if="{{mat.rules[st.code].mode === 'all'}}" class="input-row"><input class="input" type="number" value="{{mat.rules[st.code].all}}" data-field="rules.{{st.code}}.all" bindinput="setMat" placeholder="保质期" /><text class="unit-suffix">{{mat.rules[st.code].unit === 'month' ? '个月' : '天'}}</text></view>
        <view wx:if="{{mat.rules[st.code].mode === 'band'}}" class="input-row">
          <input class="input" type="number" value="{{mat.rules[st.code].warm}}" data-field="rules.{{st.code}}.warm" bindinput="setMat" placeholder="高温档" />
          <input class="input" type="number" value="{{mat.rules[st.code].cold}}" data-field="rules.{{st.code}}.cold" bindinput="setMat" placeholder="低温档" />
          <text class="unit-suffix">{{mat.rules[st.code].unit === 'month' ? '个月' : '天'}}</text></view>
        <view wx:if="{{mat.rules[st.code].mode === 'all' || mat.rules[st.code].mode === 'band'}}" class="seg unit-seg">
          <view class="chip-sq {{mat.rules[st.code].unit === 'day' ? 'on' : ''}}" data-field="rules.{{st.code}}.unit" data-value="day" bindtap="setMat">按天</view>
          <view class="chip-sq {{mat.rules[st.code].unit === 'month' ? 'on' : ''}}" data-field="rules.{{st.code}}.unit" data-value="month" bindtap="setMat">按月</view></view>
      </view>
      <view class="btn primary btn-block {{saving ? 'disabled' : ''}}" bindtap="onSaveMat">{{saving ? '保存中…' : '保存'}}</view>
      <view wx:if="{{mat.id}}" class="btn danger btn-block" bindtap="onDisableMat">停用该食材</view>
    </view>
  </scroll-view>
</van-popup>
<fnb-tabbar active="cats" />
