<view class="page">

  <view wx:if="{{loading}}" class="loading-state">加载中…</view>
  <view wx:elif="{{items.length == 0}}" class="loading-state">暂无可购买的次卡</view>
  <block wx:else>
    <view class="feed-list">
      <view class="feed-card" wx:for="{{items}}" wx:key="_key" data-index="{{index}}" bindtap="onCardTap">
        <!-- 有商品图就用图，没配图退回原来的渐变色块 + 大字次数，存量没图的商品不会开天窗 -->
        <view wx:if="{{item.imageUrl}}" class="feed-banner feed-banner--image">
          <image class="feed-banner-img" src="{{item.imageUrl}}" mode="widthFix" lazy-load="{{true}}"
            data-index="{{index}}" binderror="onBannerImageError" />
          <text class="feed-banner-tag feed-banner-tag--onimage">{{item.bizType}}{{item.cardType}}</text>
        </view>
        <view wx:else class="feed-banner feed-banner--{{item.theme}}">
          <text class="feed-banner-tag">{{item.bizType}}{{item.cardType}}</text>
          <view class="feed-banner-count-row">
            <block wx:if="{{item.isSeason}}">
              <text class="feed-banner-count feed-banner-count--season">季卡</text>
            </block>
            <block wx:else>
              <text class="feed-banner-count">{{item.punch_total}}</text>
              <text class="feed-banner-unit">次</text>
            </block>
          </view>
        </view>
        <view class="feed-body">
          <view class="feed-name">{{item.name}}</view>
          <view wx:if="{{item.desc}}" class="feed-desc">{{item.desc}}</view>
          <view class="feed-bottom-row">
            <view class="feed-price-group">
              <text class="feed-price">{{item.priceStr}}</text>
              <text wx:if="{{item.unitPriceStr}}" class="feed-unit-price">约{{item.unitPriceStr}}/次</text>
            </view>
            <view class="feed-buy-btn" catchtap="onBuyTap" data-index="{{index}}">购买</view>
          </view>
        </view>
      </view>
    </view>
  </block>

</view>
