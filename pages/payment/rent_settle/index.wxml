<view class="page-root">
  <view class="main">
    <order-summary-card wx:if="{{orderId}}" orderId="{{orderId}}"/>
    <order-payment wx:if="{{orderId}}" orderId="{{orderId}}" bind:paid="onPaid"/>
  </view>
</view>
